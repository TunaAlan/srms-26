services:
  client-mobile:
    build:
      context: ./client-mobile
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    volumes:
      - ./client-mobile:/app
      - /app/node_modules
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
